# SafeLink ML Pipeline — Python dependencies
# Tested on Python 3.10+

# Core ML
tensorflow>=2.14.0
tf_keras>=2.14.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Progress bar
tqdm>=4.65.0

# URLBert conversion (primary path)
torch>=2.1.0
transformers>=4.35.0
ai-edge-torch>=0.1.1

# URLBert conversion (fallback path)
onnx>=1.14.0

# Optional — for faster training on GPU
# tensorflow[and-cuda]>=2.14.0
