<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeLink">
        <!-- Main entry point -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Accessibility setup screen -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" /> <!-- Verdict popup — translucent overlay -->
        <activity
            android:name=".ui.VerdictPopupActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.SafeLink.Translucent"
            android:windowSoftInputMode="stateHidden" /> <!-- No internet popup -->
        <activity
            android:name=".ui.NoInternetPopupActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.SafeLink.Translucent" /> <!-- Scan history -->
        <activity
            android:name=".ui.ScanHistoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" /> <!-- Scan detail -->
        <activity
            android:name=".ui.ScanDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanHistoryActivity" /> <!-- URL intercept accessibility service -->
        <service
            android:name=".service.URLInterceptService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>