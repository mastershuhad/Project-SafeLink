<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required to list installed browsers on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeLink">

        <!-- Main entry point -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-launch setup screen -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" />

        <!-- Intercepts http/https links when SafeLink is the default browser -->
        <activity
            android:name=".ui.InterceptActivity"
            android:exported="true"
            android:theme="@style/Theme.SafeLink.Translucent"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Verdict popup — translucent overlay -->
        <activity
            android:name=".ui.VerdictPopupActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.SafeLink.Translucent"
            android:windowSoftInputMode="stateHidden" />

        <!-- No internet popup -->
        <activity
            android:name=".ui.NoInternetPopupActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.SafeLink.Translucent" />

        <!-- Manual URL tester -->
        <activity
            android:name=".ui.ManualTestActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Scan history -->
        <activity
            android:name=".ui.ScanHistoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Scan detail -->
        <activity
            android:name=".ui.ScanDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanHistoryActivity" />

    </application>

</manifest>
